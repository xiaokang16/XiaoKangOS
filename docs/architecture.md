# XiaoKangOS 通俗易懂架构解析

## 先用一个比喻来理解

想象一下，XiaoKangOS就像一个**披着网页外衣的操作系统**。

普通电脑是这样的：
```
用户 → Windows/桌面图标 → 程序 → 电脑硬件
```

XiaoKangOS是这样的：
```
用户 → 网页界面 → 浏览器 → 系统API → 电脑硬件
```

简单说，就是**用网页来代替传统的Windows桌面**！

---

## 整体架构 - 就像搭积木

XiaoKangOS由5大"积木"组成，每块积木各司其职：

```
┌─────────────────────────────────────────────────────┐
│                    第5层：应用层                      │
│   （你写的网页：HTML页面 + JavaScript程序）          │
└──────────────────────┬──────────────────────────────┘
                       │ 我要读写文件！
┌──────────────────────▼──────────────────────────────┐
│                   第4层：API层                       │
│       （xiaojs - 翻译官，翻译网页命令给系统）        │
└──────────────────────┬──────────────────────────────┘
                       │ 转成系统能看懂的话
┌──────────────────────▼──────────────────────────────┐
│                  第3层：桥梁层                       │
│    （xiao_syscall - 传达室，把命令传给内核）         │
└──────────────────────┬──────────────────────────────┘
                       │ 执行命令
┌──────────────────────▼──────────────────────────────┐
│                  第2层：内核层                       │
│           （Linux内核 - 真正管事的大管家）            │
└──────────────────────┬──────────────────────────────┘
                       │ 操作硬件
┌──────────────────────▼──────────────────────────────┐
│                  第1层：硬件层                       │
│            （CPU、内存、硬盘、显卡等）               │
└─────────────────────────────────────────────────────┘
```

---

## 每一层详细解释

### 第1层：硬件层（地基 🧱）

电脑的各种零件：
- **CPU** - 电脑的大脑，负责计算
- **内存** - 短期记忆，程序运行时用
- **硬盘** - 长期记忆，存文件
- **显卡** - 显示画面，玩游戏靠它
- **网卡** - 上网用的

**在XiaoKangOS中**：这些硬件都会被Linux内核识别和管理。

---

### 第2层：Linux内核（核心 🧠）

Linux内核是整个系统的"大脑"，负责：
- 管理所有硬件
- 运行程序
- 管理文件
- 网络通信

**XiaoKangOS的特别之处**：
- 使用定制的小型Linux内核（只有几MB，启动超快）
- 启用了显卡加速，看视频、3D更流畅
- 裁剪掉了不需要的功能，节省资源

---

### 第3层：桥梁层（传达室 📬）

这层是XiaoKangOS特有的！它是一个**Linux内核模块**，叫`xiao_syscall.ko`。

它的作用就像公司的"传达室"：
- 网页说："帮我读一下 /home/xiao/笔记.txt"
- 传达室转告内核："有人要读文件"
- 内核读完文件
- 传达室把结果拿回来给网页

**传达室还会做安全检查**：
- ❌ "你要读 /proc/密码？不行！"
- ✅ "你要读 /home/xiao/笔记.txt？允许！"

---

### 第4层：API层（翻译官 🧑‍💼）

xiaojs API就是给网页用的"翻译官"。

网页说中文（JavaScript），它翻译成Linux内核能听懂的命令。

**5大翻译官**：
| 翻译官 | 职责 | 例子 |
|--------|------|------|
| xiao.fs | 文件翻译官 | 读写文件 |
| xiao.process | 进程翻译官 | 查看/结束程序 |
| xiao.system | 系统翻译官 | 开关机 |
| xiao.network | 网络翻译官 | 查看IP |
| xiao.hardware | 硬件翻译官 | 查看CPU/内存 |

---

### 第5层：应用层（网页界面 🌐）

这就是你看到的界面！一个网页应用，包含：
- **HTML** - 页面的结构（按钮、文字、图片）
- **CSS** - 页面的样式（颜色、布局、动画）
- **JavaScript** - 页面的逻辑（点击按钮做什么）

XiaoKangOS的网页包含：
- 仪表盘（显示CPU、内存使用情况）
- 文件管理器（浏览文件）
- 进程管理器（看运行的程序）
- 设置（改系统配置）

---

## 数据是怎么流动的？

### 举个例子：网页要读取文件

**步骤1**：用户在网页点击"读取文件"按钮
```
用户点击 → HTML按钮 → JavaScript处理
```

**步骤2**：JavaScript调用xiaojs API
```javascript
xiao.fs.read('/home/xiao/笔记.txt')
```

**步骤3**：xiaojs把命令发给桥梁层
```
JavaScript → xiaojs → /proc/xiao/bridge
```

**步骤4**：桥梁层（内核模块）收到命令
```
内核模块读取 /proc/xiao/bridge → 分析要做什么
```

**步骤5**：内核模块检查权限
```
检查：用户有权限读这个文件吗？
- ✅ 允许 → 执行
- ❌ 拒绝 → 返回错误
```

**步骤6**：执行真正的文件操作
```
调用内核的 sys_read() 函数
```

**步骤7**：把结果层层传回去
```
内核 → 内核模块 → xiaojs → JavaScript → 显示给用户
```

---

## 浏览器扮演什么角色？

在XiaoKangOS中，浏览器不只是"上网的工具"，而是**整个系统的界面**！

### 特殊模式：Kiosk（ kioskMode ）

Kiosk模式 = 极简模式
- ❌ 没有地址栏
- ❌ 没有书签栏
- ❌ 没有标签页
- ❌ 没有浏览器按钮
- ✅ 全屏显示
- ✅ 只能看一个网页

这样用户感觉不到自己在用"浏览器"，就像在使用一个专门的电脑程序！

### Chromium浏览器

XiaoKangOS用的是Chromium（Chrome的核心）：
- 启动快
- 省内存
- 支持WebGL（3D显示）
- 支持硬件加速

---

## PHP后端是干嘛的？

网页除了显示界面，有时还需要：
- 用户登录/注册
- 保存数据到数据库
- 处理复杂业务逻辑

这时候就需要**后端程序**来处理，XiaoKangOS用的是PHP。

### 工作流程

```
网页(前端)                    PHP(后端)                 数据库
    │                           │                        │
    │──── 登录请求 ────────────>│                        │
    │                           │──── 查询用户 ──────────>│
    │                           │<─── 用户信息 ──────────│
    │<─── 登录成功 ────────────│                        │
    │                           │                        │
```

### PHP做的具体事情
- 用户注册、登录、登出
- 保存用户的设置
- 处理文件上传/下载
- 生成动态网页内容

---

## ISO是什么？光盘？

ISO不是一个缩写，你可以理解为**"系统镜像"**。

### 通俗解释

想象一下：
- 你的电脑硬盘里有一个Windows系统
- 你用工具把它"打包"成一个文件
- 这个文件就是ISO

这个ISO文件：
- 可以刻成光盘/U盘
- 可以在虚拟机里运行
- 安装到任何电脑上就能用

**XiaoKangOS就是一个已经打包好的ISO**，下载后：
- 烧录到U盘 → 开机就能用
- 导入虚拟机 → 立刻启动
- 安装到硬盘 → 变成真正的系统

---

## 启动流程 - 开机发生什么？

按下电源键后：

```
⚡ 电源开启
   │
   ▼
🔌 BIOS自检（硬件检查）
   │
   ▼
💾 GRUB引导（选择启动系统）
   │
   ▼
📦 内核加载（Linux系统启动）
   │
   ▼
🔧 系统初始化（驱动加载、网络启动）
   │
   ▼
🚀 浏览器自动启动（进入全屏模式）
   │
   ▼
🌐 网页界面出现（系统就绪！）
```

**目标：30秒内完成以上所有步骤！**

---

## 持久化 - 重启后数据还在吗？

普通电脑：数据存在硬盘，重启后还在
XiaoKangOS：需要配置！

### 问题

ISO系统是从光盘/ISO文件启动的，每次重启都会"回到原点"（光盘里的内容和原来一样）。

### 解决：持久化分区

在硬盘上划一个专门的分区，用来保存：
- 浏览器的历史记录
- 用户的文件
- 系统的设置

**这样重启后，你的数据还在！**

---

## 总结 - 一句话概括

**XiaoKangOS = 用Linux内核 + 定制浏览器 + 网页界面 = 一个用网页当桌面的操作系统**

用户看到的全是网页，但底层是完整的Linux系统！
